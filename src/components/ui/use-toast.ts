
// Since this file is just re-exporting from the hooks directory, we shouldn't need to modify it
// Just importing and exporting the components from the hooks directory
import { useToast, toast } from "@/hooks/use-toast";

export { useToast, toast };
